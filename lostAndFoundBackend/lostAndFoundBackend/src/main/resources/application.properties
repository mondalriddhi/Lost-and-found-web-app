spring.application.name=lostAndFoundBackend

# Database Connection
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.gfoecvylwfgthurbvijm
spring.datasource.password=Lost*2025
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
# "update" will automatically create tables based on your Java classes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Temporary Security Config (So you can test without logging in yet)
# We will change this when we build the Login system
spring.security.user.name=admin
spring.security.user.password=admin123
